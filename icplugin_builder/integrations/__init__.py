"""External integrations.

Wrappers over the ``insight-plugin`` CLI, the Kiro CLI (LLM provider), the
Docker build/validate engine, the InsightConnect tenant API, the production
plugin source provider, and the tooling update manager.
"""
